# remote extensions on by default
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --enable-remote-extensions"

# load all of the extensions installed to /usr/share/browser/extensions
export CHROMIUM_FLAGS="$CHROMIUM_FLAGS --load-extension=`ls -dm /usr/share/browser/extensions/* 2>/dev/null | tr -d '\n'`"
